[project]
name = "faers-agent"
version = "0.1.0"
description = "FAERS inverse-signal detector for drug repurposing"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "certifi>=2024.8.30",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
]
